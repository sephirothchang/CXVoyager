> **许可证**：本文档依据 [GNU GPLv3](../LICENSE) 授权发布。

# 虚拟网络sheet
集群名称 B3
管理虚拟交换机名称 C4
存储虚拟交换机名称 C6
备份虚拟交换机名称 C7
业务虚拟交换机名称 C8

默认管理虚拟网络名称 D4
额外管理组件虚拟网络名称 D5
存储虚拟网络名称 D6
备份虚拟网络名称 D7
业务虚拟网络名称 D8-D18

默认管理 vlan id F4
额外管理组件 vlan id F5
存储 vlan id F6
备份 vlan id F7
业务 vlan id F8-F18

默认管理 vlan类型 G4
额外管理组件 vlan类型 G5
存储 vlan类型 G6
备份 vlan类型 G7
业务 vlan类型 G8-G18

默认管理子网 H4
额外管理组件子网 H5
存储子网 H6
备份子网 H7
业务子网 H8-H18

默认管理网关 I4
额外管理组件网关 I5
存储网关 I6
备份网关 I7
业务网关 I8-I18

管理交换机主机网卡 J4
存储交换机主机网卡 J6
备份交换机主机网卡 J7
业务交换机主机网卡 J8

管理交换机网口绑定模式 K4
存储交换机网口绑定模式 K6
备份交换机网口绑定模式 K7
业务交换机网口绑定模式 K8

# 主机规划sheet
集群名称 C3（该值应与虚拟网络sheet的集群名称一致）
集群VIP I19
主机带外管理ip  E3-E18（最多16台主机，每台主机一个IP，此处应该展开为16个hostip变量，后续和主机相关的类似情况均如此，每个主机的不同参数应该匹配为一组）
主机带外用户名 F3-F18 （默认值ADMIN）
主机带外密码 G3-G18（默认值ADMIN）
SMTX OS主机名 H3-H18
管理地址 I3-I18
存储地址 N3-N18
集群功能 O19
主机SSH用户名 J3-J18（默认值smartx）
主机SSH密码 K3-K18（默认值HC!r0cks）
fisheye集群管理员用户名 L19（默认值root）
fisheye集群管理员密码 M19（默认值HC!r0cks）
集群序列号 E19（初始值应为空或无意义XXXX连续字符，部署后应采集真实序列号并写入表格该位置）


# 集群管理信息sheet
cloudtower ip E3
cloudtower 虚拟机名称 B3（用于平台上的虚拟机命名，强制转换为全小写，不修改guest os 内部hostname，默认值cloudtower）
cloudtower web管理员root用户密码 G3（默认值HC!r0cks）
Cloudtower 序列号 M4
OBS ip E4
OBS 虚拟机名称 B4（用于平台上的虚拟机命名，强制转换为全小写，不修改guest os 内部hostname，默认值observability）
OBS 序列号 M5
备份ip E5
备份虚拟机名称 B5（用于平台上的虚拟机命名，强制转换为全小写，不修改guest os 内部hostname，默认值backup）
备份 序列号 M5
ER控制器集群VIP E6
ER控制器节点IP E7-E11（最多5个节点IP；如果为3个有效IP，则实际部署的控制器数量为3）
ER控制器节点序列号 M6（集群使用同一个序列号）

NTP服务器 E22-E24
DNS服务器 E25-E26
临时测试IP E18-E19（此处应为IP范围，E18为起始IP，E19为结束IP，用于后续的测试虚拟机）


# 文档描述sheet
cloudtower 组织名称 C15（默认值SMTX-HCI）
cloudtower 数据中心名称 C17（默认值SMTX-HCI-DC）